#include <stdio.h>
/**
 * main - Entry point
 * Description: 'prints base10 numbers'
 * Return: 0 Always
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
